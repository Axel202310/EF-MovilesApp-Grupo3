<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asipion.pfmoviles">

    <!-- Permiso necesario para acceder a Internet, especialmente importante para Firebase Auth -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PFMoviles">

        <!-- Declaración de todas las actividades de tu aplicación -->

        <!-- Actividad de Bienvenida - Pantalla inicial (Launcher) -->
        <activity android:name=".BienvenidaActividad" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividades del flujo de Registro -->
        <activity android:name=".RegistroActividad" />
        <activity android:name=".RegistroCorreoActividad" />
        <activity android:name=".RegistroContrasenaActividad" />

        <!-- Actividades del flujo de Inicio de Sesión -->
        <activity android:name=".IniciarSesionCorreoActividad" />
        <activity android:name=".IniciarSesionContrasenaActividad" />

        <!-- ¡Actividades que faltaban y causaban el problema! -->
        <!-- Declara SeleccionarDivisaActividad -->
        <activity android:name=".SeleccionarDivisaActividad" />
        <!-- Declara IngresarSaldoActividad -->
        <activity android:name=".IngresarSaldoActividad" />
        <!-- Fin de actividades que faltaban -->

        <!-- Actividad de Inicio principal (después del onboarding/login) -->
        <activity android:name=".InicioActivity" />

        <!-- Posible MainActivity, si la usas -->
        <activity android:name=".MainActivity" />

        <!-- Es una buena práctica declarar también los servicios, receptores, etc.
             que tu app pueda usar, como los de Firebase. Firebase SDKs a menudo
             añaden las declaraciones necesarias automáticamente si usas
             el plugin google-services, pero a veces puede ser necesario revisarlas o añadirlas.
             Por ejemplo:
             <service
                android:name="com.google.firebase.components.ComponentDiscoveryService"
                android:exported="false">
                <meta-data android:name="com.google.firebase.components:com.google.firebase.auth.FirebaseAuthRegistrar"
                    android:value="com.google.firebase.components.ComponentRegistrar" />
                <meta-data android:name="com.google.firebase.components:com.google.firebase.firestore.FirestoreRegistrar"
                    android:value="com.google.firebase.components.ComponentRegistrar"/>
                ... otros componentes de Firebase que uses ...
            </service>
             -->

    </application>

</manifest>
