<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asipion.pfmoviles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PFMoviles">

        <!-- Pantalla de bienvenida: LAUNCHER -->
        <activity
            android:name=".BienvenidaActividad"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla de registro -->
        <activity android:name=".RegistroActividad" />

        <!-- Pantalla para ingresar correo -->
        <activity android:name=".IngresarCorreoActividad" />

        <!-- MainActivity (la puedes usar más adelante si es necesaria) -->
        <activity android:name=".MainActivity" />

    </application>
</manifest>
