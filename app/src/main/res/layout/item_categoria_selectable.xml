<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/contenedor_categoria"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="8dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <ImageView
        android:id="@+id/icono_categoria"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@drawable/educacion"
        android:background="@drawable/circulo_gris"
        android:contentDescription="@string/icono_categoria_content_desc"
        android:padding="12dp"
        android:scaleType="centerInside"/>

    <TextView
        android:id="@+id/nombre_categoria"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Categoría"
        android:textColor="@android:color/white"
        android:textSize="12sp"
        android:layout_marginTop="4dp"
        android:maxLines="1"
        android:ellipsize="end"/>
</LinearLayout>